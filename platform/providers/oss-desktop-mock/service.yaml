apiVersion: v1
kind: Service
metadata:
  name: epydios-oss-desktop-provider
spec:
  selector:
    app.kubernetes.io/name: epydios-oss-desktop-provider
    app.kubernetes.io/component: provider
  ports:
    - name: http
      port: 8080
      targetPort: http
