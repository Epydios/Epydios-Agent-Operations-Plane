apiVersion: v1
kind: Service
metadata:
  name: openfang-provider-windows
spec:
  selector:
    app.kubernetes.io/name: openfang-provider-windows
  ports:
    - name: https
      port: 8443
      targetPort: https
